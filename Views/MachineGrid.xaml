<UserControl x:Class="ShopDeck.Views.MachineGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- barre de recherche -->
        <DockPanel Grid.Row="0" Margin="0,0,0,14">
            <!-- import global: fichiers du PC -> cle, N machines, dossier libre -->
            <Button x:Name="ImportBtn" DockPanel.Dock="Right" Content="  ＋ Ajouter des documents  "
                    Height="38" Margin="8,0,0,0" Padding="10,0" Cursor="Hand" FontSize="13"
                    Background="{StaticResource Accent}" Foreground="White" BorderThickness="0"
                    Click="ImportBtn_Click"/>
            <Button x:Name="AddMachineBtn" DockPanel.Dock="Right" Content="  ＋ Machine  "
                    Height="38" Margin="12,0,0,0" Padding="10,0" Cursor="Hand" FontSize="13"
                    Background="{StaticResource Surface}" Foreground="{StaticResource Accent}"
                    BorderBrush="{StaticResource Accent}" BorderThickness="1"
                    ToolTip="Ajouter une machine (clic droit sur une carte pour modifier/supprimer)"
                    Click="AddMachineBtn_Click"/>
            <Border Background="{StaticResource Surface}"
                    BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="8"
                    Padding="12,8">
                <Grid>
                    <TextBlock Text="🔍  Rechercher une machine (71HTR116, 040-116, 793F, VIN...)"
                               Foreground="{StaticResource TextDim}" x:Name="Placeholder"
                               IsHitTestVisible="False" VerticalAlignment="Center"/>
                    <TextBox x:Name="SearchBox" BorderThickness="0" Background="Transparent"
                             Foreground="{StaticResource Text}" VerticalAlignment="Center"
                             TextChanged="SearchBox_TextChanged"/>
                </Grid>
            </Border>
        </DockPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="Cards">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
